# GPU port (docs/warp_port.md). Separate venv: python -m venv .venv-warp && .venv-warp/bin/pip install -r requirements-warp.txt
# mujoco-warp 3.13 requires mujoco >= 3.12; the CPU pins (3.10 / 3.11) stay in the other requirements files.
mujoco==3.13.0
mujoco-warp==3.13.0
warp-lang==1.17.0
so101-nexus==0.5.1
gymnasium==1.3.0
mjviser==0.0.14
numpy==2.5.1
torch==2.13.0
