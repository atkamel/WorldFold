[pytest]
testpaths = tests cloth_angles/tests mujuco/tests
markers =
    slow: needs a MuJoCo sim and takes seconds to minutes (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
